import React, { useState, useEffect, useCallback } from 'react'
import {
  StyleSheet,
  Pressable,
  FlatList,
  Text,
  View
} from 'react-native'


const ListItem = (props) => {
  return (<View>
    
    </View>
    )
}

export default ListItem